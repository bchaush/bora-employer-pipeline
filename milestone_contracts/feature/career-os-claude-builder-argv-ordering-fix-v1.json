{
  "milestone_id": "CAREER_OS_CLAUDE_BUILDER_ARGV_ORDERING_FIX_V1",
  "kind": "IMPLEMENTATION",
  "goal": "Fix the reproduced defect in real_claude_builder_invoker(): the prompt positional argument, placed after --allowedTools (a Commander.js variadic option), is silently consumed as an additional allowed-tool value, leaving the Claude Code CLI with no positional prompt and causing every real builder invocation to fail with 'Input must be provided either through stdin or as a prompt argument when using --print'. Reorder the constructed argv so the prompt always precedes --allowedTools (and any other variadic option), while preserving every other existing controller safety property, tool restriction, JSON output behavior, timeout, and retry/authority boundary unchanged.",
  "baseline_sha": "693d276d00b240c90926dd70209493266754b0f5",
  "allowed_paths": [
    "src/milestone_run.py",
    "tests/milestone_run_v1_test.py",
    "milestone_contracts/feature/career-os-claude-builder-argv-ordering-fix-v1.json"
  ],
  "forbidden_paths": [
    "BLUEPRINT.md",
    "AGENTS.md",
    "CLAUDE.md",
    "GEMINI.md",
    "project_state.json",
    "schemas/project_state.schema.json",
    "schemas/milestone_contract.schema.json",
    "schemas/milestone_run.schema.json",
    "schemas/milestone_reviewer_result.schema.json",
    "src/career_os_state.py",
    "scripts/verify_milestone_state.py",
    "scripts/verify_assurance_baseline.py",
    "scripts/career_os_milestone.py",
    "config/milestone_execution_policy_v1.json",
    "prompts/milestone_builder_v1.md",
    "prompts/milestone_reviewer_v1.md",
    ".github/workflows/assurance-baseline.yml",
    "src/job_analysis.py",
    "src/job_decision.py",
    "src/requirement_source_role.py",
    "src/application_logic.py",
    "src/qualification_gate.py",
    "src/application_gate.py",
    "src/resume_validation.py",
    "schemas/job.schema.json",
    "schemas/job_analysis_result.schema.json",
    "schemas/resume_derivative.schema.json",
    "evidence/**",
    "claims/**",
    "resume/**",
    "golden-tests/**",
    "tests/career_os_state_v1_test.py"
  ],
  "required_tests": [
    "tests/milestone_run_v1_test.py",
    "tests/career_os_state_v1_test.py"
  ],
  "acceptance_conditions": [
    "real_claude_builder_invoker() constructs argv with the prompt positioned before --allowedTools and before any other variadic option, so the Claude Code CLI never consumes the prompt as a tool-name value.",
    "The prompt is delivered intact regardless of embedded spaces, newlines, braces, quotes, or command-like text.",
    "--allowedTools continues to restrict tools to exactly the same existing set.",
    "--output-format json remains present.",
    "--permission-mode remains unchanged (acceptEdits).",
    "Timeout behavior (policy['builder_timeout_seconds']) remains unchanged.",
    "A nonzero subprocess exit or malformed output still raises InfrastructureError (fail closed), unchanged.",
    "A new regression exercises the subprocess boundary with a fake Claude-like executable/shim that models the real CLI's variadic --allowedTools argument-swallowing behavior and proves the prompt is received as the actual positional prompt, not swallowed as a tool value -- not merely a static argv-list assertion.",
    "python tests/milestone_run_v1_test.py passes in full.",
    "python tests/career_os_state_v1_test.py passes in full, unmodified.",
    "python scripts/verify_milestone_state.py passes for this branch.",
    "python scripts/verify_assurance_baseline.py reports ALL PHASES PASSED.",
    "git diff --check is clean.",
    "No controller state-machine semantics, reviewer behavior, execution policy, or audit/product logic changed."
  ],
  "stop_conditions": [
    "origin/main or repository reality is incompatible with the authorized baseline",
    "implementation requires any path outside allowed_paths or any forbidden/protected surface",
    "the fix would require redesigning the builder adapter, adding a provider, or switching to a broader invocation mechanism (e.g. shell=True, stdin piping) when the minimal argv-reorder correction suffices",
    "implementation requires changing reviewer behavior, controller state-machine semantics, or execution policy",
    "reviewer finding is materially disputed or semantically ambiguous and cannot be resolved deterministically",
    "a new third-party runtime dependency or credential architecture would be required"
  ],
  "review_requirements": "CURSOR_INDEPENDENT_ADVERSARIAL_READ_ONLY_REVIEW_REQUIRED_BEFORE_READY_FOR_HUMAN_APPROVAL",
  "human_approval_requirements": "BORA_EXPLICIT_APPROVAL_REQUIRED_AFTER_REVIEW_AND_FULL_ASSURANCE; NO_COMMIT_PUSH_PR_OR_MERGE_UNTIL_CURSOR_SAFE_AND_BORA_AUTHORIZATION"
}
